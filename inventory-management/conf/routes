GET        /v1/phones                com.jotason.phone.v1.inventory.controllers.InventoryController.list
GET        /v1/phones/:id            com.jotason.phone.v1.inventory.controllers.InventoryController.get(id: Int)

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file        controllers.Assets.at(path="/public", file)

